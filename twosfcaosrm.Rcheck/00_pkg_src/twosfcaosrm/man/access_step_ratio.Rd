% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/access-manuscript.R
\name{access_step_ratio}
\alias{access_step_ratio}
\title{Step-ratio access scores (\eqn{R = 1/\sum P}, \eqn{A} per 10\,000)}
\usage{
access_step_ratio(
  pnts,
  iso,
  supply,
  access_name,
  supply_id = "query",
  geoid_col = "GEOID"
)
}
\arguments{
\item{pnts}{\code{sf} of supplies with \code{within_isochrone} (list column) and
\code{query} (or \code{supply_id}).}

\item{iso}{\code{sf} or data with tract id column \code{GEOID} (or \code{geoid_col}).}

\item{supply}{Attribute table merged by supply id (must include \code{population} for demand weights).}

\item{access_name}{Name for the output column (e.g. \code{"access_5"}).}

\item{supply_id}{Column name for pharmacy / facility id on \code{pnts}.}

\item{geoid_col}{Tract id column on \code{iso} (default \code{"GEOID"}).}
}
\value{
A tibble with \code{geoid} and one column \code{access_name}.
}
\description{
For each supply site, \eqn{R = 1/\sum P} over demand zones whose isochrone
contains that site (using \code{population}). For each zone, \eqn{A = \sum R \times
10000}. Inputs match the usual pipeline: \code{pnts} with list column
\code{within_isochrone} from \code{\link[sf:geos_binary_pred]{sf::st_within()}}, \code{iso} with one isochrone row per zone
(same row order as indices in \code{within_isochrone}), and \code{supply} with
per-site \code{population} for the join. For \eqn{R_j = S_j / \sum_i P_i}{Rj = Sj/sum(Pi)} use \code{\link[=access]{access()}} or
\code{\link[=twosfca_scores]{twosfca_scores()}}.
}
