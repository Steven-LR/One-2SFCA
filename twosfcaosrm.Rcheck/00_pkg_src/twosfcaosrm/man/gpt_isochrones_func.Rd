% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/isochrones-manuscript.R
\name{gpt_isochrones_func}
\alias{gpt_isochrones_func}
\title{Multi-band isochrones with \code{safely} (gpt-style)}
\usage{
gpt_isochrones_func(data, lat, lng, total_minutes, interval, server, profile)
}
\arguments{
\item{data}{\code{sf} with coordinates in columns given by \code{lat} and \code{lng} (\strong{unquoted}).}

\item{lat, lng}{bare column names.}

\item{total_minutes, interval}{Passed to \code{seq()}.}

\item{server, profile}{OSRM connection.}
}
\value{
\code{sf} with original attribute columns (no geometry from input) plus
isochrone geometry and \code{row_id}.
}
\description{
Builds breaks \code{seq(0, total_minutes, by = interval)} and stacks results with
\code{row_id} for joining back to attributes.
}
