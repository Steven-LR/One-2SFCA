% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catchments.R
\name{iso_nearest_data_prep}
\alias{iso_nearest_data_prep}
\title{Prepare OD tables after \code{st_within} list columns (nearest-neighbor style prep)}
\usage{
iso_nearest_data_prep(
  supply_with_zones,
  zone_sf,
  supply_lookup = NULL,
  supply_id_col = "query"
)
}
\arguments{
\item{supply_with_zones}{A data frame or \code{sf} with \code{query} (or \code{supply_id_col})
and \code{within_isochrone} list column from \code{\link[sf:geos_binary_pred]{sf::st_within()}}.}

\item{zone_sf}{\code{sf} polygons whose row order defines \code{within_isochrone}
indices (same as passed to \code{st_within}).}

\item{supply_lookup}{Optional additional columns merged by supply id.}

\item{supply_id_col}{Supply key column name.}
}
\value{
A tibble ready for pairwise OSRM calls.
}
\description{
Expands \code{within_isochrone} integer indices (row positions in \code{zone_sf}) into
long form and attaches zone centroids as \code{longitude_c} / \code{latitude_c} for use
with \code{\link[=duration_url]{duration_url()}} or \code{\link[=distance_url]{distance_url()}}.
}
