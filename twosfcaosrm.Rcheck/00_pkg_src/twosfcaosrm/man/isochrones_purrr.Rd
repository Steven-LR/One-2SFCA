% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/isochrones-manuscript.R
\name{isochrones_purrr}
\alias{isochrones_purrr}
\title{Manuscript-style isochrones (\code{purrr::map2} + \code{unnest})}
\usage{
isochrones_purrr(data, lat, lng, minutes, server, profile)
}
\arguments{
\item{data}{An \code{sf} object (geometry dropped before OSRM calls).}

\item{lat, lng}{Names of latitude/longitude columns.}

\item{minutes}{Single break at \code{minutes} (see \code{breaks = seq(0, minutes, by = minutes)}).}

\item{server}{OSRM base URL.}

\item{profile}{OSRM profile.}
}
\value{
\code{sf} with one polygon per successful request.
}
\description{
Matches the common pattern in thesis code: drop geometry, nest isochrone
\code{sf} objects per row, \code{unnest}, set geometry. Column names \code{lat} and \code{lng}
are \strong{strings} (e.g. \code{"latitude"}, \code{"longitude"}).
}
