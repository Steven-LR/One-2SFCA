% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/acs-lwc.R
\name{lwc_access}
\alias{lwc_access}
\title{Monte Carlo accessibility with propagated ACS uncertainty (LWC)}
\usage{
lwc_access(
  data,
  pnts_c,
  pnts_w,
  iso_c,
  iso_w,
  n_sims,
  keep = NULL,
  supply_id_col = "query",
  tract_id_col = "GEOID",
  pop_col = "population",
  acs_num_est = "estimate_car_truck_van",
  acs_num_moe = "moe_car_truck_van",
  acs_den_est = "estimate_total_trans",
  acs_den_moe = "moe_total_trans"
)
}
\arguments{
\item{data}{Tract table (see \code{\link[=join_iso]{join_iso()}}).}

\item{pnts_c, pnts_w}{\code{sf} point layers with \code{supply_id_col} and \code{within_isochrone}.}

\item{iso_c, iso_w}{Isochrone \code{sf} objects with \code{tract_id_col} aligned to row order
of \code{within_isochrone} indices.}

\item{n_sims}{Number of Monte Carlo draws.}

\item{keep}{Character vector of \code{tract_id_col} values to force into each simulation
summary (tracts missing from a draw get \eqn{A = 0} and \eqn{p = 0} after
\code{left_join}). If \code{NULL}, uses \code{unique(data[[tract_id_col]])}.}

\item{supply_id_col}{Facility identifier (default \code{"query"}).}

\item{tract_id_col}{Small-area id in \code{data} and on isochrones (default \code{"GEOID"}).}

\item{pop_col}{Population column on \code{data} (default \code{"population"}).}

\item{acs_num_est, acs_num_moe}{ACS estimate and MOE for the \strong{numerator} count
(e.g. vehicle commuters).}

\item{acs_den_est, acs_den_moe}{ACS estimate and MOE for the \strong{denominator} count
(e.g. workers traveling to work).}
}
\value{
Tibble with columns \code{tract_id_col}, \code{mean_A}, \code{median_A}, \code{q0.025}, \code{q0.975},
\code{mean_p}, \code{median_p}, \code{q0.025_p}, \code{q0.975_p}. Rename as needed (e.g. \code{median_A} → \code{lwc_A5}).
}
\description{
Repeats: build mode-specific support from car/walk isochrones (\code{\link[=join_iso]{join_iso()}}),
draw ACS-consistent stochastic counts (\code{\link[=acs_gen]{acs_gen()}}), recompute \eqn{R} (\code{\link[=R_func]{R_func()}})
and tract accessibility \eqn{A} (\code{\link[=A_func]{A_func()}}). Summarizes the \eqn{n_{\mathrm{sims}}}
realizations per tract: mean/median \eqn{A}, 2.5\% and 97.5\% quantiles, and the same
for the realized mode-share \eqn{p}. Use this when accessibility depends on decomposing
population demand using published ACS estimates with MOEs (e.g. commute mode shares).
}
