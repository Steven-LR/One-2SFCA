% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catchments.R
\name{match_supplies_to_isochrones}
\alias{match_supplies_to_isochrones}
\title{Build demand--supply pairs from point-in-polygon tests}
\usage{
match_supplies_to_isochrones(
  supply_sf,
  isochrones_sf,
  supply_id_col = "query",
  demand_id_col = "GEOID",
  crs = 3857
)
}
\arguments{
\item{supply_sf}{\code{sf} points with a unique supply identifier column.}

\item{isochrones_sf}{\code{sf} polygons with a demand identifier column (e.g.
tract \code{GEOID}).}

\item{supply_id_col}{Name of the supply id column in \code{supply_sf}.}

\item{demand_id_col}{Name of the demand id column in \code{isochrones_sf}.}

\item{crs}{CRS to use for predicates (projected CRS recommended). If \code{NULL},
both layers must already share the same CRS.}
}
\value{
A tibble with columns \code{supply_id}, \code{demand_id}.
}
\description{
For each supply site, finds all demand zones (isochrone polygons) whose
geometry contains that site after an optional common projection. This matches
the common workflow: isochrones are built from demand centroids; pharmacies
(supplies) are tested with \code{\link[sf:geos_binary_pred]{sf::st_within()}}.
}
