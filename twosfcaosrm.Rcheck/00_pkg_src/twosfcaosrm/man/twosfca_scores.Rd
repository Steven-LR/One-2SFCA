% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/twosfca.R
\name{twosfca_scores}
\alias{twosfca_scores}
\alias{twosfca_from_pairs}
\title{Two-step floating catchment accessibility scores}
\usage{
twosfca_scores(
  pairs,
  demand_tbl,
  supply_tbl,
  demand_key_col = "GEOID",
  supply_key_col = "query",
  pop_col = "population",
  supply_col = NULL,
  default_supply = 1
)

twosfca_from_pairs(
  pairs,
  demand_tbl,
  supply_tbl,
  demand_key_col = "GEOID",
  supply_key_col = "query",
  pop_col = "population",
  supply_col = NULL,
  default_supply = 1
)
}
\arguments{
\item{pairs}{Long-form table with columns \code{supply_id} and \code{demand_id} (as
returned by \code{\link[=match_supplies_to_isochrones]{match_supplies_to_isochrones()}}).}

\item{demand_tbl}{Table of demand: must include the same id column name as
\code{pairs$demand_id} uses for joining (typically name passed as
\code{demand_key_col}) and a population column.}

\item{supply_tbl}{Table of supplies: must include supply ids matching
\code{pairs$supply_id}. If \code{supply_col} is \code{NULL} or missing from \code{supply_tbl},
every supply uses \code{default_supply}.}

\item{demand_key_col}{Name of the demand zone id column in \code{demand_tbl}
(e.g. \code{"GEOID"}).}

\item{supply_key_col}{Name of the supply id column in \code{supply_tbl}.}

\item{pop_col}{Name of the population / demand column in \code{demand_tbl}.}

\item{supply_col}{Name of the supply-capacity column in \code{supply_tbl}, or
\code{NULL} to use \code{default_supply}.}

\item{default_supply}{Scalar capacity when \code{supply_col} is not used.}
}
\value{
A tibble with columns \code{demand_key_col} (same name as input) and \code{A}.
}
\description{
Implements the standard \strong{2SFCA} logic (Luo & Wang 2003):
}
\details{
\strong{Step 1}: For each supply site \eqn{j}, compute the ratio
\deqn{R_j = S_j / \sum_{i \in D_j} P_i}
where \eqn{D_j} is the set of demand zones whose catchment contains supply
\eqn{j}, \eqn{P_i} is demand (population), and \eqn{S_j} is supply capacity.

\strong{Step 2}: For each demand zone \eqn{i},
\deqn{A_i = \sum_{j \in S_i} R_j}
where \eqn{S_i} is the set of supplies inside zone \eqn{i}'s catchment.
}
\references{
Luo, W., & Wang, F. (2003). Measures of spatial accessibility to health care
in a GIS environment. \emph{International Journal of Geographical Information
Science}, 17(1), 15--27.
}
