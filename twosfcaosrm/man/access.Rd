% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/access.R
\name{access}
\alias{access}
\alias{gpt_access}
\title{Join 2SFCA accessibility scores to demand polygons}
\usage{
access(
  pnts,
  isochrones,
  supply,
  name_col,
  demand_id_col = "GEOID",
  supply_id_col = "query",
  pop_col = "population",
  crs_for_match = 3857,
  default_supply = 1,
  supply_weight_col = NULL
)

gpt_access(...)
}
\arguments{
\item{pnts}{\code{sf} point layer of supplies (e.g. facilities). Must contain
\code{supply_id_col}.}

\item{isochrones}{\code{sf} polygons with \code{demand_id_col}, \code{pop_col}, and any
other attributes you wish to retain in the output.}

\item{supply}{Non-spatial attribute table for supplies (e.g. row-level
capacity). Must contain \code{supply_id_col}. Rows need not match \code{pnts}
one-to-one; unique supply ids are used.}

\item{name_col}{Name of the new numeric column (e.g. \code{"access_5"}).}

\item{demand_id_col}{Zone id on \code{isochrones} (e.g. \code{"GEOID"}).}

\item{supply_id_col}{Supply key on \code{pnts} and \code{supply} (e.g. \code{"query"} or
\code{"name"}).}

\item{pop_col}{Demand weight (usually population).}

\item{crs_for_match}{Projected CRS for \code{\link[sf:geos_binary_pred]{sf::st_within()}} (e.g. \code{3857} or a
local state plane like \code{2263} for NYC-area work).}

\item{default_supply}{Capacity when no weight column is provided.}

\item{supply_weight_col}{Optional column name on \code{supply} for Step-1
numerators \eqn{S_j}.}

\item{...}{Passed to \code{\link[=access]{access()}}.}
}
\value{
\code{sf} object = \code{isochrones} plus one new column \code{name_col}.
}
\description{
Runs \code{\link[=match_supplies_to_isochrones]{match_supplies_to_isochrones()}}, then \code{\link[=twosfca_scores]{twosfca_scores()}}, and
left-joins the result to \code{isochrones} so you keep census-tract geometry.
This mirrors the \strong{\verb{access_*} pattern} used in manuscript workflows where
pharmacy points (\code{pnts}) carry a \code{st_within} match against isochrone
polygons.
}
\details{
\code{gpt_access} is an alias kept for compatibility with older analysis
scripts; behavior is identical to \code{\link[=access]{access()}}.
}
\seealso{
\code{\link[=access_step_ratio]{access_step_ratio()}} for the thesis-style ratio
(\eqn{R=1/\sum P}, \eqn{A} per 10k) used with pharmacy data.
}
