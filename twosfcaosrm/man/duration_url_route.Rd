% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/route-http.R
\name{distance_url_route}
\alias{distance_url_route}
\alias{duration_url_route}
\title{Pairwise durations via OSRM \strong{Route} API (loop; one HTTP GET per row)}
\usage{
distance_url_route(
  data,
  scale_fac = 1.60934 * 1000,
  lat1,
  lng1,
  lat2,
  lng2,
  profile,
  server
)

duration_url_route(data, lat1, lng1, lat2, lng2, profile, server)
}
\arguments{
\item{data}{A data frame.}

\item{scale_fac}{Meters per mile (OSRM route distance is in meters).}

\item{lat1, lng1, lat2, lng2}{Character names of latitude / longitude columns.}

\item{profile}{Routing profile (e.g. \code{"driving"}, \code{"foot"} — must match OSRM).}

\item{server}{Base URL (with or without trailing slash).}
}
\value{
\code{data} with a \code{duration} column.
}
\description{
Unlike \code{\link[=duration_url]{duration_url()}}, which uses \code{\link[osrm:osrmTable]{osrm::osrmTable()}} on the full table,
this matches manuscript \code{duration_url} behavior against a Docker OSRM
\code{route/v1} endpoint—useful for throttling or debugging single pairs.
}
