% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/access-manuscript.R
\name{gpt_access_bands}
\alias{gpt_access_bands}
\title{\code{gpt_access}: wide table of access by isochrone band (\code{isomax})}
\usage{
gpt_access_bands(
  pnts,
  iso,
  supply,
  access_name,
  supply_id = "query",
  geoid_col = "GEOID"
)
}
\arguments{
\item{pnts}{\code{sf} of supplies with \code{within_isochrone} (list column) and
\code{query} (or \code{supply_id}).}

\item{iso}{Isochrone \code{sf} with \code{GEOID} and \code{isomax}.}

\item{supply}{Attribute table merged by supply id (must include \code{population} for demand weights).}

\item{access_name}{Name for the output column (e.g. \code{"access_5"}).}

\item{supply_id}{Column name for pharmacy / facility id on \code{pnts}.}

\item{geoid_col}{Tract id column on \code{iso} (default \code{"GEOID"}).}
}
\value{
Wide tibble: \code{geoid}, columns \code{paste0(access_name, "_", isomax)}.
}
\description{
Expects \code{iso} to include an \code{isomax} column (as returned by some OSRM
isochrone workflows). If absent, a constant column \code{isomax = 1} is added.
}
