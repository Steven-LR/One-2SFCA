% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/osrm-local.R
\name{isochrones_func}
\alias{isochrones_func}
\title{Isochrones in the style used in workflow scripts (one ring per row)}
\usage{
isochrones_func(
  df,
  lat_col,
  lon_col,
  minutes,
  osrm_base_url,
  profile,
  crs = 4326
)
}
\arguments{
\item{df}{Input data frame.}

\item{lat_col, lon_col}{Latitude / longitude column \strong{names} (character).}

\item{minutes}{One positive number (e.g. \code{5} for a five-minute shell).}

\item{osrm_base_url}{Base URL of the running OSRM service, e.g.
\code{"http://127.0.0.1:5001/"}.}

\item{profile}{Routing profile, e.g. \code{"car"} or \code{"foot"}.}

\item{crs}{Output CRS; passed to \code{\link[sf:st_transform]{sf::st_transform()}}. Use \code{NULL} to skip.}
}
\value{
\code{sf} object with original columns plus polygon geometry.
}
\description{
Convenience wrapper: builds a single travel-time ring at \code{minutes} (via
\verb{breaks = seq(0, minutes, by = minutes))}, drops to an \code{sf} polygon layer,
and optionally reprojects.
}
