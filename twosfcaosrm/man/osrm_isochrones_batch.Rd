% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/osrm-local.R
\name{osrm_isochrones_batch}
\alias{osrm_isochrones_batch}
\alias{osrm_isodistance_batch}
\title{Batch isochrones from a local OSRM server}
\usage{
osrm_isochrones_batch(
  x,
  lon_col,
  lat_col,
  breaks,
  osrm_base_url,
  profile,
  keep_failures = FALSE
)

osrm_isodistance_batch(
  x,
  lon_col,
  lat_col,
  max_distance_meters,
  osrm_base_url,
  profile,
  keep_failures = FALSE
)
}
\arguments{
\item{x}{A data frame with longitude and latitude columns (see \code{lon_col},
\code{lat_col}).}

\item{lon_col, lat_col}{Column names for WGS84 coordinates.}

\item{breaks}{Passed to \code{\link[osrm:osrmIsochrone]{osrm::osrmIsochrone()}} (e.g. \code{seq(0, 5, by = 5)}
for a 5-minute contour only).}

\item{osrm_base_url}{Base URL of the running OSRM service, e.g.
\code{"http://127.0.0.1:5001/"}.}

\item{profile}{Routing profile, e.g. \code{"car"} or \code{"foot"}.}

\item{keep_failures}{If \code{FALSE}, rows with routing errors are dropped.}

\item{max_distance_meters}{Maximum network distance in meters (single break).}
}
\value{
An \code{sf} object (same rows as \code{x} that succeeded, or all if
\code{keep_failures}, with isochrone geometry).
}
\description{
Calls \code{\link[osrm:osrmIsochrone]{osrm::osrmIsochrone()}} for each demand location. Failed rows are
omitted unless \code{keep_failures} is \code{TRUE} (then \code{geometry} may be missing).
}
\examples{
\dontrun{
d <- data.frame(id = 1, lon = -74.0, lat = 40.7)
osrm_isochrones_batch(
  d, "lon", "lat",
  breaks = seq(0, 10, by = 10),
  osrm_base_url = "http://127.0.0.1:5002/",
  profile = "foot"
)
}
}
